<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="WpfHack.ValidationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Validation Result"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="420"
        MinHeight="200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="8" Background="#FFF" Padding="8">
            <Image x:Name="GifImage" Width="320" Height="240" Stretch="Uniform" />
        </Border>

        <TextBlock x:Name="MessageText"
                   Grid.Row="1"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap"
                   FontSize="16"
                   Foreground="Black" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button x:Name="CloseButton" Width="100" Padding="6,4" Click="Close_Click">Close</Button>
        </StackPanel>
    </Grid>
</Window>
